using MoSeqAcquire.Models.Configuration;
using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace MoSeqAcquire.Models.Acquisition
{
    /*public class MediaSourceConfigMediator
    {
        private List<ComplexProperty> properties;
        private Dictionary<string, Action> listners;

        public void BindConfig(MediaSourceConfig Config)
        {
            Config.PropertyChanged += Config_PropertyChanged;
        }

        private void Config_PropertyChanged(object sender, PropertyChangedEventArgs e)
        {
            if (this.listners.ContainsKey(e.PropertyName))
            {
                this.listners[e.PropertyName].Invoke();
            }
        }

        public void BindProperty<T>(string PropertyName, ComplexProperty PropertyDef)
        {
            this.listners[PropertyName] = ()=>
            {
                PropertyDef.
            }
        }
    }*/
}
