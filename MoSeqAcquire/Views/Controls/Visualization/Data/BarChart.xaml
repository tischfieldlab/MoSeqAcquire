<local:BaseChart x:Class="MoSeqAcquire.Views.Controls.Visualization.Data.BarChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MoSeqAcquire.Views.Controls.Visualization.Data"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Canvas Background="Black" Name="MainCanvas">
        <Canvas.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}" />
                <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}" />
                <Setter Property="FontSize" Value="8" />
            </Style>
            <Style TargetType="Rectangle">
                <Setter Property="Fill" Value="{DynamicResource PrimaryHueMidBrush}" />
            </Style>
        </Canvas.Resources>
    </Canvas>
</local:BaseChart>
