<Window x:Class="MoSeqAcquire.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:toolkit="clr-namespace:Microsoft.Kinect.Toolkit;assembly=Microsoft.Kinect.Toolkit"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
        xmlns:local="clr-namespace:MoSeqAcquire"
        xmlns:vm="clr-namespace:MoSeqAcquire.ViewModels"
        xmlns:v="clr-namespace:MoSeqAcquire.Views"
        xmlns:ctrl="clr-namespace:MoSeqAcquire.Views.Controls"
        xmlns:conv="clr-namespace:MoSeqAcquire.Views.Converters"
        xmlns:mio="clr-namespace:MoSeqAcquire.Models.IO"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <ResourceDictionary>
            <conv:EnumEqualityConverter x:Key="EnumEqualityConverter" />
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Menu Grid.Row="0" Grid.ColumnSpan="2" >
            <MenuItem Header="File">
                <MenuItem Header="Save Protocol" Command="{Binding Path=Commands.SaveProtocol}" />
                <MenuItem Header="Load Protocol" Command="{Binding Path=Commands.LoadProtocol}" />
            </MenuItem>
        </Menu>
        <GroupBox Header="Available Hardware" Grid.Column="0" Grid.Row="1">
            <v:MediaSourceView HorizontalAlignment="Stretch" />
        </GroupBox>

        <GroupBox Header="Recording Console" Grid.Column="1" Grid.Row="1" DataContext="{Binding Path=Recorder}">
            <StackPanel>
                <WrapPanel>
                    <Button Content="Start" Command="{Binding Path=Root.Commands.StartRecording}" />
                    <Button Content="Stop"  Command="{Binding Path=Root.Commands.StopRecording}" />
                    <ProgressBar Width="100" IsIndeterminate="{Binding Path=IsRecording}" />
                </WrapPanel>
                <WrapPanel>
                    
                    <StackPanel>
                        <Label>Recording Length</Label>
                        <RadioButton IsChecked="{Binding Path=GeneralSettings.RecordingMode, Converter={StaticResource EnumEqualityConverter}, ConverterParameter={x:Static mio:RecordingMode.FrameCount}}">Capture Frame Count</RadioButton>
                        <RadioButton IsChecked="{Binding Path=GeneralSettings.RecordingMode, Converter={StaticResource EnumEqualityConverter}, ConverterParameter={x:Static mio:RecordingMode.TimeCount}}">Capture Seconds</RadioButton>
                        <RadioButton IsChecked="{Binding Path=GeneralSettings.RecordingMode, Converter={StaticResource EnumEqualityConverter}, ConverterParameter={x:Static mio:RecordingMode.Indeterminate}}">Until Stopped</RadioButton>
                    </StackPanel>
                </WrapPanel>
                <WrapPanel>
                    <Label>Destination Directory</Label>
                    <ctrl:FileChooser PickerType="Folder" SelectedPath="{Binding Path=GeneralSettings.Directory}" />
                </WrapPanel>
                <WrapPanel>
                    <Label>Output Basename</Label>
                    <TextBox Text="{Binding Path=GeneralSettings.Basename}" />
                </WrapPanel>
                
                <GroupBox Header="Recorders" >
                    <StackPanel>
                        <WrapPanel>
                            <ComboBox ItemsSource="{Binding Path=AvailableRecorderTypes}" SelectedItem="{Binding Path=SelectedRecorderType}" />
                            <Button Content="Add Recorder" Command="{Binding Path=Root.Commands.AddRecorder}" />
                        </WrapPanel>
                        <ListBox ItemsSource="{Binding Path=Recorders}" SelectedItem="{Binding Path=SelectedRecorder}" >
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Path=DisplayName}">
                                        <TextBlock.InputBindings>
                                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding Root.Commands.EditRecorder}"/>
                                        </TextBlock.InputBindings>
                                    </TextBlock>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
